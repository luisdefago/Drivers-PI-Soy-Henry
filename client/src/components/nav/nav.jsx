import "./nav.css";

const Nav = () => {
  return (
    <header className="header">
      <nav>
        <div>
          <button>Home</button>
          <button>About me</button>
        </div>
      </nav>
    </header>
  );
};

export default Nav;
